import{Routes,Route,NavLink,useNavigate,useParams,Link,useLocation}from'react-router-dom';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import Editor from'@monaco-editor/react';import{BookOpen,ChartNoAxesCombined,Code2,Flame,Home,RotateCcw,Play,Check,Lightbulb,AlertTriangle,ChevronRight,ChevronLeft,ArrowLeft,ArrowRight,LogOut,Maximize2,Minimize2,Eye,Menu,X,Clock3,Trophy,PanelLeftClose,PanelLeftOpen,Copy,CheckCircle2,Circle,LockKeyhole}from'lucide-react';import{useEffect,useMemo,useState,useRef}from'react';import{api}from'./api';import type{Module,Progress,RunResult,Topic,Exercise}from'./types';
const modulesQ=()=>api<Module[]>('/modules');const progressQ=()=>api<Progress>('/progress');
function Layout(){const[mob,setMob]=useState(false),[collapsed,setCollapsed]=useState(()=>localStorage.getItem('koda:sidebar')==='collapsed');const{pathname}=useLocation();const focus=pathname.startsWith('/practice/');const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});const toggle=()=>setCollapsed(x=>{localStorage.setItem('koda:sidebar',x?'open':'collapsed');return!x});return <div className={`app ${focus?'focus-mode':''} ${collapsed?'sidebar-collapsed':''}`}>{!focus&&<><button className="mobile-menu" onClick={()=>setMob(!mob)}>{mob?<X/>:<Menu/>}</button><aside className={mob?'open':''}><div className="side-brand"><Link className="logo" to="/" onClick={()=>setMob(false)} title="KODA Practice"><span>K</span><b>KODA</b><i>Practice</i></Link><button className="collapse-side" onClick={toggle} aria-label={collapsed?'Развернуть меню':'Свернуть меню'}>{collapsed?<PanelLeftOpen/>:<PanelLeftClose/>}</button></div><nav>{[[Home,'Главная','/'],[Code2,'Практика','/catalog'],[BookOpen,'Темы','/catalog'],[AlertTriangle,'Ошибки','/errors'],[ChartNoAxesCombined,'Прогресс','/progress']].map(([I,t,to])=><NavLink key={String(t)} to={String(to)} title={String(t)} onClick={()=>setMob(false)} end={to==='/'}>{<I size={17}/>}<span>{String(t)}</span></NavLink>)}</nav><div className="side-stat"><small>РЕШЕНО</small><strong>{p?.solved??0} <i>/ {p?.total??60}</i></strong><div className="bar"><span style={{width:`${(p?.solved??0)/(p?.total||60)*100}%`}}/></div></div><div className="side-foot"><Flame size={16}/><span>Локальный прогресс</span></div></aside></>}<main><Routes><Route path="/" element={<Dashboard/>}/><Route path="/catalog" element={<Catalog/>}/><Route path="/topics/:slug" element={<TopicPage/>}/><Route path="/practice/:eid" element={<Practice/>}/><Route path="/errors" element={<Errors/>}/><Route path="/progress" element={<ProgressPage/>}/></Routes></main></div>}
function Header({title,crumb='pandas'}:{title:string;crumb?:string}){const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});return <header><div><small>{crumb} <ChevronRight size={12}/> {title}</small><h1>{title}</h1></div><div className="metrics"><span><Flame/> серия <b>{p?.activity.length??0}</b></span><span><Trophy/> <b>{p?.xp??0}</b> XP</span></div></header>}
function Dashboard(){const nav=useNavigate();const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});const next=p?.modules.find(m=>m.solved<m.total)?.slug||'start';return <><Header title="Продолжим практику"/><section className="page"><div className="hero"><div><span className="eyebrow">ЕЖЕДНЕВНАЯ ПРАКТИКА</span><h2>Десять минут, которые закрепляют pandas</h2><p>Короткая очередь из новой задачи, слабой темы и запланированного повторения.</p><button onClick={()=>nav(`/topics/${next}`)}><Play/> Начать практику</button></div><div className="ring"><b>{p?.solved??0}</b><small>из {p?.total??60}</small></div></div><div className="stats"><Card label="К повторению" value={p?.due??0} icon={<RotateCcw/>}/><Card label="Точность с первого раза" value={`${p?.first_try_accuracy??0}%`} icon={<Check/>}/><Card label="Самостоятельно" value={`${p?.independent_rate??0}%`} icon={<Code2/>}/></div><h3>Прогресс обучения</h3><div className="module-list">{p?.modules.slice(0,6).map(m=><Link to={`/topics/${m.slug}`} className="module-row" key={m.slug}><span>{m.title}</span><div className="bar"><i style={{width:`${m.mastery}%`}}/></div><b>{m.mastery}%</b><ChevronRight/></Link>)}</div></section></>}
function Card({label,value,icon}:{label:string;value:string|number;icon:React.ReactNode}){return <div className="card"><div className="icon">{icon}</div><small>{label}</small><strong>{value}</strong></div>}
function Catalog(){const[f,setF]=useState('all');const{data:mods=[]}=useQuery({queryKey:['modules'],queryFn:modulesQ});const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});const status=new Map(p?.modules.map(x=>[x.slug,x]));const filtered=mods.filter(m=>f==='all'||(f==='learning'&&status.get(m.slug)?.status==='learning')||(f==='weak'&&(status.get(m.slug)?.mastery??0)<50)||(f==='mastered'&&status.get(m.slug)?.status==='mastered'));return <><Header title="Каталог тем"/><section className="page"><div className="filters">{[['all','Все'],['learning','Изучаю'],['weak','Слабые'],['mastered','Освоенные']].map(x=><button className={f===x[0]?'active':''} onClick={()=>setF(x[0])} key={x[0]}>{x[1]}</button>)}</div><div className="catalog">{filtered.map((m,i)=>{const x=status.get(m.slug);return <Link to={`/topics/${m.slug}`} className="module-card" key={m.slug}><small>{String(i+1).padStart(2,'0')} · {m.topics[0].exercises.length} ЗАДАЧИ</small><h2>{m.title}</h2><p>{m.description}</p><div><div className="bar"><i style={{width:`${x?.mastery??0}%`}}/></div><b>{x?.mastery??0}%</b></div></Link>})}</div></section></>}
function CodeBlock({label,code}:{label:string;code:string}){const[copied,setCopied]=useState(false);const copy=async()=>{try{await navigator.clipboard.writeText(code);setCopied(true);setTimeout(()=>setCopied(false),1200)}catch{setCopied(false)}};return <div className="topic-code"><div><span>{label}</span><button onClick={copy}>{copied?<Check/>:<Copy/>}{copied?'Скопировано':'Копировать'}</button></div><pre>{code}</pre></div>}
function TopicPage(){const{slug=''}=useParams();const nav=useNavigate();const{data:t,error,refetch,isFetching}=useQuery({queryKey:['topic',slug],queryFn:()=>api<Topic>(`/topics/${slug}`)});const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});const{data:mods=[]}=useQuery({queryKey:['modules'],queryFn:modulesQ});if(error)return <QueryError retry={()=>refetch()} pending={isFetching}/>;if(!t)return <Loading/>;const mp=p?.modules.find(x=>x.slug===slug),solvedIds=new Set(mp?.solved_ids||[]),done=solvedIds.size,nextIndex=t.exercises.findIndex(e=>!solvedIds.has(e.id)),current=nextIndex<0?t.exercises.length-1:nextIndex,modIndex=mods.findIndex(x=>x.slug===slug);const taskNames=slug==='start'?['DataFrame из словаря','Выбор порядка столбцов','Проверка индекса и безопасная копия']:t.exercises.map((e,i)=>e.title.replace(/ · вариант \d+/,i===0?'':` · шаг ${i+1}`));const outcomes=slug==='start'?['Создавать Series и DataFrame из структур Python','Понимать разницу между копией и ссылкой','Не изменять исходные данные случайно']:[`Применять ${t.syntax} в аналитических задачах`,'Проверять форму и тип полученного результата','Сохранять преобразования без случайной потери данных'];return <><div className="topic-topbar"><div><span>pandas</span><ChevronRight/><b>{t.title}</b></div><div><span><Flame/> Серия {p?.activity.length||0}</span><span><Trophy/> {p?.xp||0} XP</span></div></div><main className="topic-page"><section className="topic-hero"><div className="topic-hero-main"><span className="topic-kicker">Теория · {t.exercises.length} задачи</span><h1>{t.title}</h1><p>{t.summary}. {t.theory}</p><div className="topic-hero-actions"><button onClick={()=>nav(`/practice/${t.exercises[current].id}`)}><Play/> {done?'Продолжить практику':'Начать практику'}</button><span>≈ {t.exercises.length*4} минут</span></div></div><aside className="topic-summary"><div><span>Прогресс темы</span><b>{Math.round(done/t.exercises.length*100)}%</b></div><div className="topic-hero-progress"><i style={{width:`${done/t.exercises.length*100}%`}}/></div><dl><div><dt>Решено</dt><dd>{done}/{t.exercises.length}</dd></div><div><dt>Доступно XP</dt><dd>{t.exercises.reduce((a,e)=>a+e.xp,0)}</dd></div></dl></aside></section><section className="topic-theory"><div className="theory-primary"><div className="section-heading"><span>01</span><div><h2>Ключевая идея</h2><p>Синтаксис и практический пример</p></div></div><CodeBlock label="Синтаксис" code={t.syntax}/><CodeBlock label="Разобранный пример" code={t.example}/></div><div className="theory-secondary"><h3>Типичные ошибки</h3><ul className="topic-mistakes">{t.mistakes.map(x=><li key={x}>{x}</li>)}</ul><h3>Методы темы</h3><div className="chips">{t.methods.map(x=><span key={x}>{x}</span>)}</div></div></section><section className="practice-route"><div className="route-heading"><div><span className="topic-kicker">Практика</span><h2>Маршрут по теме</h2></div><div><span>{done} из {t.exercises.length}</span><div className="route-progress"><i style={{width:`${done/t.exercises.length*100}%`}}/></div></div></div><div className="route-list">{t.exercises.map((e,i)=>{const solved=solvedIds.has(e.id),active=!solved&&i===current;return <Link to={`/practice/${e.id}`} className={`${solved?'solved':''} ${active?'active':''}`} key={e.id}><div className="route-marker">{solved?<CheckCircle2/>:<Circle/>}</div><div className="route-number">{String(i+1).padStart(2,'0')}</div><div className="route-copy"><b>{e.title}</b><span>Сложность {e.difficulty} · {e.xp} XP</span></div><div className="route-status">{solved?'Пройдено':active?'Продолжить':'Открыть'}</div><ArrowRight/></Link>})}</div></section><section className="topic-outcomes"><div><span className="topic-kicker">После этой темы вы сможете</span><ul>{outcomes.map(x=><li key={x}><Check/>{x}</li>)}</ul></div><nav><Link className={modIndex<=0?'disabled':''} to={modIndex>0?`/topics/${mods[modIndex-1].slug}`:'#'}><ChevronLeft/><span>Предыдущая тема<small>{modIndex>0?mods[modIndex-1].title:'Начало курса'}</small></span></Link><Link className={modIndex<0||modIndex>=mods.length-1?'disabled':''} to={modIndex>=0&&modIndex<mods.length-1?`/topics/${mods[modIndex+1].slug}`:'#'}><span>Следующая тема<small>{modIndex>=0&&modIndex<mods.length-1?mods[modIndex+1].title:'Конец курса'}</small></span><ChevronRight/></Link></nav></section></main></>}
function DataPreview({dataset}:{dataset:Record<string,unknown>}){
 const frames=useMemo(()=>{const out:Array<[string,Record<string,unknown[]>]>=[];for(const[k,v]of Object.entries(dataset)){if(k==='variables'&&v&&typeof v==='object'){for(const[vk,vv]of Object.entries(v as Record<string,unknown>))if(vv&&typeof vv==='object'&&!Array.isArray(vv))out.push([vk,vv as Record<string,unknown[]>])}else if(k==='series'&&v&&typeof v==='object'){for(const[vk,vv]of Object.entries(v as Record<string,{data?:unknown[]}>))out.push([vk,{value:vv.data??[]}])}else if(!['files'].includes(k)&&v&&typeof v==='object'&&!Array.isArray(v))out.push([k,v as Record<string,unknown[]>])}return out},[dataset]);
 const[active,setActive]=useState(0),[expanded,setExpanded]=useState(false);useEffect(()=>setActive(0),[dataset]);
 if(!frames.length)return <div className="data-empty">Для этой задачи входная таблица не требуется.</div>;
 const[name,obj]=frames[Math.min(active,frames.length-1)],cols=Object.keys(obj),n=Math.max(0,...cols.map(c=>Array.isArray(obj[c])?obj[c].length:0));
 const dtype=(values:unknown[])=>values.every(x=>x==null||typeof x==='number')?'number':values.every(x=>x==null||typeof x==='boolean')?'bool':'string';
 return <div className={`data-block ${expanded?'expanded':''}`}><div className="data-toolbar"><div className="data-tabs">{frames.map(([x],i)=><button className={i===active?'active':''} onClick={()=>setActive(i)} key={x}>{x}</button>)}</div><button className="expand-data" onClick={()=>setExpanded(!expanded)}>{expanded?<Minimize2/>:<Maximize2/>}{expanded?'Свернуть':'Развернуть'}</button></div><div className="table-wrap input-table"><table><thead><tr><th>#<small>index</small></th>{cols.map(c=><th key={c}>{c}<small>{dtype(obj[c] as unknown[])}</small></th>)}</tr></thead><tbody>{Array.from({length:n},(_,i)=><tr key={i}><td>{i}</td>{cols.map(c=><td key={c}>{String((obj[c] as unknown[])?.[i]??'NaN')}</td>)}</tr>)}</tbody></table></div></div>
}
function ResultPreview({result}:{result:NonNullable<RunResult['result']>}){if(result.kind==='dataframe'){const rows=result.data as unknown[][];return <div className="table-wrap result-table"><table><thead><tr>{result.columns?.map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows?.slice(0,12).map((row,i)=><tr key={i}>{row.map((x,j)=><td key={j}>{String(x)}</td>)}</tr>)}</tbody></table></div>}if(result.kind==='series')return <div className="series-preview">{(result.data as unknown[])?.map((x,i)=><div key={i}><span>{result.index?.[i]??i}</span><b>{String(x)}</b></div>)}</div>;if(result.kind==='plot')return <div className="feedback success compact"><Check/><div><h3>График создан</h3><p>Runner обнаружил объект визуализации.</p></div></div>;return <pre>{JSON.stringify(result.data,null,2)}</pre>}
function ResultView({r}:{r:RunResult}){if(!r.ok||r.passed===false){const x=r.explanation;return <div className="feedback error detailed"><AlertTriangle/><div><h3>{x?.title||'Ошибка проверки'}</h3><p>{x?.what||r.error}</p>{x?.where&&<pre className="error-location">{x.where}</pre>}{x?.python_error&&<p><b>Python:</b> {x.python_error}</p>}{x?.expected&&x?.actual&&<div className="compare"><span><small>Ожидалось</small><code>{x.expected}</code></span><span><small>Получено</small><code>{x.actual}</code></span></div>}{x?.difference&&<p className="difference">{x.difference}</p>}<b>Следующий шаг</b><pre className="error-check">{x?.check}</pre>{x?.hint&&<em>{x.hint}</em>}<small>{r.execution_ms} мс</small></div></div>}if(!r.result)return <div className="empty">Запустите код, чтобы увидеть результат.</div>;const shape=r.result.kind==='dataframe'?`${(r.result.data as unknown[][])?.length||0} × ${r.result.columns?.length||0}`:r.result.kind==='series'?`${(r.result.data as unknown[])?.length||0}`:'—';return <div className="run-output">{r.stdout&&<div className="stdout"><b>stdout</b><pre>{r.stdout}</pre></div>}<div className="result-meta"><span>Тип <b>{r.result.kind}</b></span><span>Shape <b>{shape}</b></span><span>Время <b>{r.execution_ms} мс</b></span></div><ResultPreview result={r.result}/></div>}
function Practice(){
 const{eid=''}=useParams();const nav=useNavigate(),qc=useQueryClient(),splitRef=useRef<HTMLDivElement>(null);const{data:e,error:exerciseError,refetch:refetchExercise,isFetching}=useQuery({queryKey:['exercise',eid],queryFn:()=>api<Exercise>(`/exercises/${eid}`)});const{data:mods=[]}=useQuery({queryKey:['modules'],queryFn:modulesQ});
 const match=eid.match(/(.+)-(\d+)$/),slug=match?.[1]||'',number=Number(match?.[2]||1),topic=mods.flatMap(m=>m.topics).find(t=>t.slug===slug),total=topic?.exercises.length||3,moduleTitle=topic?.title||slug;
 const[code,setCode]=useState(''),[result,setResult]=useState<RunResult|null>(null),[hints,setHints]=useState<string[]>([]),[hintsOpen,setHintsOpen]=useState(true),[solution,setSolution]=useState('');const[left,setLeft]=useState(()=>Number(localStorage.getItem('koda:left-pane'))||43),[editorH,setEditorH]=useState(()=>Number(localStorage.getItem('koda:editor-height'))||430);
 useEffect(()=>{if(e){setCode(e.starter_code);setResult(null);setHints([]);setSolution('')}},[e]);
 const action=useMutation({mutationFn:({submit}:{submit:boolean})=>api<RunResult>(submit?'/attempts/submit':'/executions/run',{method:'POST',body:JSON.stringify({exercise_id:eid,code})}),onSuccess:(r,vars)=>{setResult(r);if(vars.submit)qc.invalidateQueries({queryKey:['progress']})}});
 useEffect(()=>{const f=(x:KeyboardEvent)=>{if((x.ctrlKey||x.metaKey)&&x.key==='Enter'&&!action.isPending){x.preventDefault();action.mutate({submit:x.shiftKey})}};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[code,action.isPending]);
 if(exerciseError)return <QueryError retry={()=>refetchExercise()} pending={isFetching} focus/>;if(!e)return <Loading/>;const moduleIndex=mods.findIndex(m=>m.slug===slug),go=(n:number)=>{if(n<1)return nav(`/topics/${slug}`);if(n<=total)return nav(`/practice/${slug}-${String(n).padStart(3,'0')}`);const next=mods[moduleIndex+1];nav(next?`/practice/${next.topics[0].exercises[0].id}`:'/progress')};const hint=async()=>{if(hints.length<3&&!action.isPending){const x=await api<{content:string}>(`/exercises/${eid}/hints/${hints.length+1}`,{method:'POST'});setHints([...hints,x.content]);setHintsOpen(true)}};const reveal=async()=>{const x=await api<{solution:string}>(`/exercises/${eid}/solution`,{method:'POST'});setSolution(x.solution)};
 const dragColumns=(ev:React.PointerEvent)=>{ev.currentTarget.setPointerCapture(ev.pointerId);const move=(x:PointerEvent)=>{const r=splitRef.current?.getBoundingClientRect();if(r){const v=Math.max(35,Math.min(55,(x.clientX-r.left)/r.width*100));setLeft(v);localStorage.setItem('koda:left-pane',String(v))}};const up=()=>{removeEventListener('pointermove',move);removeEventListener('pointerup',up)};addEventListener('pointermove',move);addEventListener('pointerup',up)};
 const dragRows=(ev:React.PointerEvent)=>{ev.currentTarget.setPointerCapture(ev.pointerId);const start=ev.clientY,base=editorH;const move=(x:PointerEvent)=>{const v=Math.max(220,Math.min(650,base+x.clientY-start));setEditorH(v);localStorage.setItem('koda:editor-height',String(v))};const up=()=>{removeEventListener('pointermove',move);removeEventListener('pointerup',up)};addEventListener('pointermove',move);addEventListener('pointerup',up)};
 return <div className="practice"><div className="focus-header"><button className="icon-btn" onClick={()=>nav(`/topics/${slug}`)} aria-label="Назад к теме"><ArrowLeft/></button><div className="crumbs"><b>pandas</b><span>/</span><span>{moduleTitle}</span></div><div className="task-position"><span>Задача {number} из {total}</span><div className="topic-progress"><i style={{width:`${number/total*100}%`}}/></div></div><div className="focus-xp"><Trophy/>+{e.xp} XP</div><div className="task-nav"><button onClick={()=>go(number-1)} disabled={number===1} aria-label="Предыдущая задача"><ChevronLeft/></button><button onClick={()=>go(number+1)} disabled={number===total} aria-label="Следующая задача"><ChevronRight/></button></div><button className="exit-focus" onClick={()=>nav(`/topics/${slug}`)}><LogOut/>Выйти</button></div>
 <div className="workspace resizable" ref={splitRef} style={{gridTemplateColumns:`minmax(360px,${left}fr) 8px minmax(460px,${100-left}fr)`}}><section className="problem"><span className="eyebrow">ЗАДАЧА · СЛОЖНОСТЬ {e.difficulty}</span><h1>{e.title}</h1><p>{e.instructions}</p><div className="target">Сохраните результат в <code>result</code></div><div className="section-title"><h3>Исходные данные</h3></div><DataPreview dataset={e.dataset}/><div className="hints"><div className="hint-head"><span><Lightbulb/> Использовано {hints.length} из 3</span>{hints.length>0&&<button className="ghost" onClick={()=>setHintsOpen(!hintsOpen)}>{hintsOpen?'Свернуть':'Развернуть'}</button>}</div>{hintsOpen&&hints.map((x,i)=><div className="hint-item" key={x}><b>{i+1}</b><p>{x}</p></div>)}{hints.length<3?<button className="hint-next" onClick={hint} disabled={action.isPending}>Открыть подсказку {hints.length+1}</button>:!solution?<button className="show-solution" onClick={reveal}><Eye/> Показать решение</button>:<pre className="revealed-solution">{solution}</pre>}</div></section><div className="col-divider" onPointerDown={dragColumns}/><section className="solution"><div className="editor-head"><div><b>solution.py</b><small>Python 3.12 · pandas</small></div><button className="ghost" onClick={()=>setCode(e.starter_code)} disabled={action.isPending}><RotateCcw/> Сбросить</button></div><div className="editor-area" style={{height:editorH}}><Editor height="100%" language="python" theme="vs-dark" value={code} onChange={x=>setCode(x||'')} options={{fontSize:14,fontFamily:'JetBrains Mono',minimap:{enabled:false},padding:{top:16},scrollBeyondLastLine:false}}/></div><div className="row-divider" onPointerDown={dragRows}/><div className="actions"><button className="secondary" onClick={()=>action.mutate({submit:false})} disabled={action.isPending}>{action.isPending?<Clock3/>:<Play/>} Запустить <kbd>Ctrl/Cmd+Enter</kbd></button><button onClick={()=>action.mutate({submit:true})} disabled={action.isPending}>{action.isPending?<Clock3/>:<Check/>} Проверить <kbd>Ctrl/Cmd+Shift+Enter</kbd></button></div><div className="result"><div className="tabs"><b>Результат</b></div>{action.isPending?<Loading/>:result?<ResultView r={result}/>:<div className="empty">Результат выполнения появится здесь</div>}{result?.passed&&<div className="feedback success solved"><Check/><div><h3>Все тесты пройдены</h3><div className="success-stats"><span>+{result.xp_earned} XP</span><span>Попыток: {result.attempt_number}</span><span>Подсказок: {result.hints_used}</span><span>{result.execution_ms} мс</span></div><p>{result.approach}</p><button onClick={()=>go(number+1)}>Следующая задача <ChevronRight/></button></div></div>}</div></section></div></div>
}
function Errors(){const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});return <><Header title="Работа над ошибками"/><section className="page"><p className="lead">Здесь собраны последние неудачные попытки и задачи, которые стоит пройти повторно.</p>{!p?.recent_errors.length?<div className="empty big"><Check/> Ошибок пока нет. Отличное начало.</div>:<div className="exercise-list">{p.recent_errors.map(x=><Link to={`/practice/${x.exercise_id}`} key={x.id}><AlertTriangle/><span>{x.exercise_id}<small>{x.error_type||'Неверный ответ'} · {new Date(x.created_at).toLocaleString('ru')}</small></span><ChevronRight/></Link>)}</div>}</section></>}
function ProgressPage(){const{data:p}=useQuery({queryKey:['progress'],queryFn:progressQ});return <><Header title="Прогресс"/><section className="page"><div className="stats"><Card label="Решено задач" value={`${p?.solved??0}/${p?.total??60}`} icon={<Check/>}/><Card label="Попыток" value={p?.attempts??0} icon={<Code2/>}/><Card label="XP" value={p?.xp??0} icon={<Trophy/>}/></div><h3>Освоение модулей</h3><div className="module-list">{p?.modules.map(m=><Link to={`/topics/${m.slug}`} className="module-row" key={m.slug}><span>{m.title}<small>{m.solved} из {m.total}</small></span><div className="bar"><i style={{width:`${m.mastery}%`}}/></div><b>{m.mastery}%</b></Link>)}</div><h3>Активность</h3><div className="activity">{Array.from({length:28},(_,i)=>{const d=new Date();d.setDate(d.getDate()-27+i);const x=p?.activity.find(a=>a.day===d.toISOString().slice(0,10));return <span key={i} title={`${d.toLocaleDateString('ru')}: ${x?.attempts||0}`} className={x?.attempts?'hot':''}/>})}</div></section></>}
function QueryError({retry,pending,focus=false}:{retry:()=>void;pending:boolean;focus?:boolean}){return <div className={`query-error ${focus?'focus-error':''}`}><AlertTriangle/><h2>Не удалось подключиться к KODA API</h2><p>Проверьте, что backend запущен командой <code>npm run dev</code>, затем повторите запрос.</p><button onClick={retry} disabled={pending}>{pending?<Clock3/>:<RotateCcw/>}{pending?'Подключаемся…':'Повторить'}</button><Link to="/catalog">Вернуться в каталог</Link></div>}
function Loading(){return <div className="loading"><Clock3/> Загрузка…</div>}export default Layout;
